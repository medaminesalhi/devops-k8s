- name: Installer MySQL Server
  apt:
    name: mysql-server
    state: present
    update_cache: yes
  when: tools_install_mysql

- name: Démarrer et activer MySQL
  systemd:
    name: mysql
    enabled: yes
    state: started
  when: tools_install_mysql
